# Phase 3 Wave 2 Implementation Plan (Requisition, Corrigendum, Doc Verification)

> **For agentic workers:** Use subagent-driven-development or executing-plans. Steps use `- [ ]`.

**Goal:** Requisition → approval workflow, advertisement corrigenda with auto-notify, structured document verification. Spec: `docs/superpowers/specs/2026-08-08-phase3-wave2-design.md`.

## Global Constraints
- New models tenant-schema; `migrate_schemas` at final verification.
- All status changes audited via `log_audit`.
- Python: `.venv/Scripts/python.exe`; pytest `--reuse-db` + retry. Commits: `Phase 3: Requisition — …`, `Phase 3: Corrigendum — …`, `Phase 3: DocVerify — …`.

---

### Task 1: Requisition models + migration
**Files:** `recruitment/models.py` (append 2 models), `recruitment/migrations/0014_*.py`, `recruitment/test_requisition.py` (create)

**Interfaces:** `VacancyRequisition` (post_name, count, grade, justification, status, created_by FK null, created_at), `RequisitionApproval` (requisition FK, stage, approver FK null, decision, comments, timestamp).

- [ ] **Step 1: Failing test**: `VacancyRequisition.objects.create(...)` + `RequisitionApproval` FK round-trip.
- [ ] **Step 2: Run** — FAIL.
- [ ] **Step 3: Implement** — append models, makemigrations.
- [ ] **Step 4: Run** — PASS.
- [ ] **Step 5: Commit** — `Phase 3: Requisition — models + migration`

### Task 2: Requisition views + workflow
**Files:** `recruitment/views.py` (+ requisition_list/create/detail/approve), `recruitment/urls.py` (+routes), `templates/recruitment/requisition_*.html` (create), `recruitment/test_requisition.py` (append)

- [ ] **Step 1: Failing tests**: create requisition (hr_manager) → status draft; submit → submitted; approve (finance_officer) → finance_approved; reject with comments → rejected.
- [ ] **Step 2: Run** — FAIL.
- [ ] **Step 3: Implement** — views per spec (role-gated per stage); audit on each approval/rejection.
- [ ] **Step 4: Run** — PASS.
- [ ] **Step 5: Commit** — `Phase 3: Requisition — approval workflow views + templates`

### Task 3: Corrigendum model + views
**Files:** `recruitment/models.py` (append Corrigendum), `recruitment/migrations/0015_*.py`, `recruitment/views.py` (+ corrigendum_create), `templates/recruitment/advertisement_detail.html` (corrigenda section), `recruitment/test_corrigendum.py` (create)

**Interfaces:** `Corrigendum` (advertisement FK, version PositiveIntegerField, changes_text, published_date, is_active, created_at). Unique on (advertisement, version).

- [ ] **Step 1: Failing tests**: create corrigendum → version auto-bumps; listing shows it on advt detail; auto-notify creates outbox rows.
- [ ] **Step 2: Run** — FAIL.
- [ ] **Step 3: Implement** — model, view (hr_manager POST), template section, notification wiring.
- [ ] **Step 4: Run** — PASS.
- [ ] **Step 5: Commit** — `Phase 3: Corrigendum — model + views + notification`

### Task 4: DocumentVerification model + views
**Files:** `recruitment/models.py` (append DocumentVerification), `recruitment/migrations/0016_*.py`, `recruitment/views.py` (+ verify_documents + dashboard), `recruitment/urls.py` (+routes), `templates/recruitment/verify_documents.html` (create), `recruitment/test_verify.py` (create)

**Interfaces:** `DocumentVerification` (document OneToOne, verifier FK null, status pending/verified/rejected, comments, verified_at).

- [ ] **Step 1: Failing tests**: auto-create on Document creation; verify → status verified + AuditEvent; reject → rejected + audit.
- [ ] **Step 2: Run** — FAIL.
- [ ] **Step 3: Implement** — model, signal (auto-create), views (recruiter role), audit.
- [ ] **Step 4: Run** — PASS.
- [ ] **Step 5: Commit** — `Phase 3: DocVerify — structured verification workflow`

### Task 5: Final verification
- [ ] `migrate_schemas` (0014-0016 applied).
- [ ] `pytest -q` — full suite green.
- [ ] E2E — green.
- [ ] Push.
