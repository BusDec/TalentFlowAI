# Phase 3 Wave 3 Implementation Plan

> **For agentic workers:** Use subagent-driven-development. Steps use `- [ ]`.

**Goal:** Interview panel + scheduling + score aggregation, fairness dashboard (4/5ths rule + parity), court case tracker with litigation banner. Spec: `docs/superpowers/specs/2026-08-08-phase3-wave3-design.md`.

## Global Constraints
- New models tenant-schema; `migrate_schemas` at final verification.
- All status changes audited via `log_audit`.
- Python: `.venv/Scripts/python.exe`; pytest `--reuse-db` + retry. Commits: `Phase 3: Interview — …`, `Phase 3: Fairness — …`, `Phase 3: Litigation — …`.

---

### Task 1: Interview models + migration
**Files:** `recruitment/models.py` (append 3 models), `recruitment/migrations/0018_*.py`, `recruitment/test_interview.py` (create)

**Interfaces:** `InterviewPanel` (post FK, name, members M2M, external_members JSONField, sitting_fee Decimal null, created_at), `InterviewSlot` (panel FK, application FK, datetime, duration_minutes default 30, status, notes), `InterviewScore` (slot FK, panel_member FK null, score Decimal(4,1), comments, created_at).

- [ ] **Step 1: Failing tests**: create panel + slot + score round-trip; slot status transitions.
- [ ] **Step 2: Run** — FAIL.
- [ ] **Step 3: Implement** — models, makemigrations.
- [ ] **Step 4: Run** — PASS.
- [ ] **Step 5: Commit** — `Phase 3: Interview — models + migration`

### Task 2: Interview views + auto-schedule
**Files:** `recruitment/views.py` (+ 4 views), `recruitment/urls.py`, templates, `recruitment/test_interview.py` (append)

- [ ] **Step 1: Failing tests**: constitute panel (hr_manager), schedule slot (recruiter), enter score, view aggregate results.
- [ ] **Step 2: Run** — FAIL.
- [ ] **Step 3: Implement** — views per spec; auto-schedule signal on Application→interview.
- [ ] **Step 4: Run** — PASS.
- [ ] **Step 5: Commit** — `Phase 3: Interview — views + scheduling + score aggregation`

### Task 3: Fairness engine + dashboard
**Files:** `agents/fairness.py` (create), `recruitment/views.py` (+ fairness_dashboard), `recruitment/urls.py`, `agents/test_fairness.py` (create)

**Interfaces:** `agents.fairness.compute_adverse_impact(selection_rates_by_category) -> dict` (4/5ths check), `compute_statistical_parity(selection_rates) -> dict` (disparity).

- [ ] **Step 1: Failing tests**: 4/5ths flag (SC 40% vs UR 60% → flagged), parity metrics, prompt sanitization check.
- [ ] **Step 2: Run** — FAIL.
- [ ] **Step 3: Implement** — pure functions in `agents/fairness.py`; view aggregates from CategoryAllocation + Application; audit the resume_evaluator SYSTEM_PROMPT for demographic stripping (add pre-processing if needed).
- [ ] **Step 4: Run** — PASS.
- [ ] **Step 5: Commit** — `Phase 3: Fairness — adverse impact + statistical parity + dashboard`

### Task 4: Litigation model + views + application blocker
**Files:** `recruitment/models.py` (append LitigationCase), `recruitment/migrations/0019_*.py`, `recruitment/views.py` (+ litigation views), `templates/recruitment/application_detail.html` (litigation banner), `recruitment/test_litigation.py` (create)

**Interfaces:** `LitigationCase` (case_number, court, petitioner, application/post/advertisement FKs null, status, interim_orders JSONField, final_order_text, filed_on, resolved_on null, created_at).

- [ ] **Step 1: Failing tests**: create case with stay_order → banner on application detail; add interim order; audit.
- [ ] **Step 2: Run** — FAIL.
- [ ] **Step 3: Implement** — model, views (hr_manager), banner (amber alert on detail page when active stay exists), audit.
- [ ] **Step 4: Run** — PASS.
- [ ] **Step 5: Commit** — `Phase 3: Litigation — court case tracker + stay order banner`

### Task 5: Final verification
- [ ] `migrate_schemas`.
- [ ] `pytest -q` — full suite green.
- [ ] E2E — green.
- [ ] Push.
