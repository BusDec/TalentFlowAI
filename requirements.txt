# --- Django ---
django>=5.0,<6.1
django-tenants>=3.6
psycopg2-binary>=2.9
python-dotenv>=1.0
djangorestframework>=3.15

# --- OCR / document ---
pytesseract>=0.3.13
Pillow>=10.0

# --- LLM client (OpenAI-compatible: DeepSeek, OpenAI, Azure, etc.) ---
openai>=1.30

# --- Phase 1: async document processing ---
celery>=5.4
redis>=5.0

# --- Phase 1: PII encryption (Fernet) ---
cryptography>=42.0

# --- Phase 4: rate limiting ---
django-ratelimit>=4.1

# --- Phase 1: test foundation ---
pytest>=8.0
pytest-django>=4.8
pytest-cov>=5.0
factory-boy>=3.3
django-storages[azure]
whitenoise>=6.5
gunicorn>=21.2
